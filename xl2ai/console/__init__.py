"""Views: where xl2ai meaning meets the drawing toolkit.

`xl2ai.ui` knows how to draw and nothing else; `xl2ai.observe` knows what happened and nothing else. This package
is the only place that knows both -- that a "stage" deserves a heading, that a reused source should read as a win
rather than as a skip, and what a person needs to see first when a run fails.
"""
