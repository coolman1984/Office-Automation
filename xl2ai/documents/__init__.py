"""Documents: free the data locked in Word, PowerPoint, PDF and e-mail (Outlook .msg, .eml) -- and plain text/HTML/CSV.

Every reader produces the same `Doc` (see model.py): located text blocks, tables, metadata and attachments. The store
turns tables into ordinary typed data tables, so everything built for Excel (catalog, key numbers, relationships,
anomalies, report checks, cross-file SQL) works on document tables too; text blocks become full-text searchable and
every value an agent extracts from them can be proven against its exact source.
"""
